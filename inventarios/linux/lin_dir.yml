- hosts: all
  tasks
  - name: list files
    find:
      paths: /var/log
      patterns: '*.old,*.log.gz'
      size: 10m
      register: files_matched

  - debug:
      msg: "{{ files_matched.files }}"
